let range,filter,map;_9d5‍.w("rxjs",[["range",["range"],function(v){range=v}],["filter",["filter"],function(v){filter=v}],["map",["map"],function(v){map=v}]]);

range(1, 200)
  .pipe(
    filter(x => x % 2 === 1),
    map(x => x + x)
  )
  .subscribe(x => _9d5‍.g.console.log(x));